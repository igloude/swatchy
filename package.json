{
  "name": "swatchy",
  "version": "0.3.0",
  "description": "A Chrome extension that pulls colors from the current web page and generates a color palette.",
  "main": "background.js",
  "scripts": {
    "clean": "rm -rf node_modules && npm install",
    "build": "npx vite build",
    "dev": "npx vite build --watch"
  },
  "author": "Ian Gloude",
  "license": "MIT",
  "dependencies": {
    "vue": "^3.5.12"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.0",
    "vite": "^5.4.11"
  }
}
